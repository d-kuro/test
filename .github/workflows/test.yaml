name: test

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_call:

jobs:
  select-env:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ls -la
